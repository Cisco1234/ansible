---
- hosts: all
  ansible_user: gaurav
  ansible_become: yes
  ansible_become_method: sudo
  ansible_become_user: root
  

  tasks:
  - name: ensure apache is at the latest version
    yum:
      name: httpd
      enablerepo: epel    
      state: present
